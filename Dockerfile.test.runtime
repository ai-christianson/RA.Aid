FROM python:3.12-slim

WORKDIR /app

# Install git
RUN apt-get update && apt-get install -y git && apt-get clean

# Copy the project files
COPY . /app/

# Install the package
RUN pip install -e .

# Set the entrypoint to run ra-aid --version
ENTRYPOINT ["ra-aid", "--version"]
